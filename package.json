{
  "name": "frida-mod",
  "version": "1.0.0",
  "description": "Test project for frida-mod",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile index.ts -o _agent.js -c",
    "watch": "frida-compile index.ts -o _agent.js -w"
  },
  "devDependencies": {
    "@types/frida-gum": "^17.1.0",
    "@types/node": "^16.4.8",
    "frida-compile": "^10.0.0"
  },
  "dependencies": {
  }
}
